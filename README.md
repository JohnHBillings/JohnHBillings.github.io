# johnhbillings.github.io

Personal academic website for John H. Billings ("Jack").

Static single-page site (no build step). Layout adapted from [tayweid.github.io](https://tayweid.github.io).

## Local preview

```bash
python3 -m http.server 8080
```

Then open http://localhost:8080

## Archive

The previous Jekyll/Minimal Mistakes site is preserved in the `jekyll-archive` git tag.
